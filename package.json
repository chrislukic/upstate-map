{
  "name": "scenic-ny-map",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --strictPort",
    "analyze": "vite build --mode analyze",
    "check:size": "node scripts/check-bundle-size.js || exit 1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}



